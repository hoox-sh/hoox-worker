{
  "name": "hoox",
  "version": "1.0.0",
  "description": "HOOX public gateway worker: TradingView webhooks, WAF/IP allowlist, rate limits, Durable Object idempotency, Service Binding dispatch to trade/telegram/analytics.",
  "main": "src/index.ts",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@hoox-sh/hoox-shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260708.1",
    "bun-types": "^1.3.14",
    "typescript": "^5.9.3",
    "wrangler": "^4.95.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hoox-sh/hoox-worker.git"
  }
}
